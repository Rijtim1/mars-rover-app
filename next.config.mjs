/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "mars.jpl.nasa.gov",
            "mars.nasa.gov",
            "apod.nasa.gov"
        ]
    }
};

export default nextConfig;
